{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Docker Compose Up Build Force",
      "type": "shell",
      "command": "docker-compose up --build --force-recreate -d",
      "options": {
        "cwd": "${workspaceFolder}",
        "statusbar": {
          "hide": false,
          "color": "#ffc400"
        }
      },
      "problemMatcher": []
    }
  ]
}